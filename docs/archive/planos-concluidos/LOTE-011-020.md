# Lotes 011–020 — Fundamentos de Operação

## Foco
Governança de schema, UX robusta e prontidão de produção.

## Lotes
11. Governança de migrações e compatibilidade.
12. UX de erro/empty/loading states.
13. Checklist LGPD por fluxo.
14. Offline básico com PWA.
15. Cache inteligente (camada app).
16. Cache orientado a eventos.
17. Notificação + email padronizados.
18. Central de preferências de notificação.
19. Auditoria de segurança web (CSP/CORS/headers).
20. Go/No-Go MVP Produção.

## Critério de saída
MVP apto para produção controlada com checklist completo aprovado.

---

## Execução Real — LOTE 011 (20/02/2026)

### Objetivo (1 frase)
Corrigir a confiabilidade operacional do backup D1 e alinhar compatibilidade de execução com ambiente Cloudflare remoto.

### Escopo técnico
- Operação/DB: ajuste do script de backup para leitura no D1 remoto.
- Governança de schema: descoberta dinâmica de tabelas para evitar acoplamento com nomes legados.
- Documentação: atualização do guia operacional com comportamento real do backup.
- Validação: reexecução do backup para confirmar redução de falhas.

### Arquivos impactados no lote
- `scripts/cf-backup.js`
- `docs/CF-OPERATIONS.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Backup D1 alterado para execução remota (`wrangler d1 execute --remote`).
- Descoberta dinâmica de tabelas de aplicação via `sqlite_master`.
- Exclusão de tabelas internas (`sqlite_%` e `_cf_%`) no dump por tabela.
- Metadados de backup passam a refletir tabelas detectadas em runtime.

### Validação obrigatória (Gate 0)
- `node scripts/cf-backup.js` ✅

### Evidência operacional
- Antes: falhas por `no such table` (consulta local e/ou tabela legada inexistente).
- Depois: backup executado com sucesso para todas as tabelas de aplicação detectadas.
- Execução validada em backup: `backups/2026-02-20T18-21-54`.

### Segurança e operação (Step 4)
- Menor risco de falso negativo no backup D1.
- Redução de fragilidade por schema drift em tabelas de seed/templates.
- Risco residual: `cf:backup` ainda depende de autenticação/permite da conta Cloudflare no ambiente local.

### Rollout / Rollback
- Rollout: imediato, sem impacto em runtime de API.
- Rollback: restaurar `scripts/cf-backup.js` para lista fixa de tabelas (não recomendado).

### Métricas de sucesso do lote
- Falhas de backup D1 reduzidas de múltiplas para zero na execução validada.
- Procedimento operacional alinhado ao ambiente real (`--remote`).

### Status do lote
- **LOTE 011: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 012 — UX robusta de erro/empty/loading states**

---

## Execução Real — LOTE 012 (20/02/2026)

### Objetivo (1 frase)
Elevar a UX de erro/empty/loading com foco em performance percebida e transições sem flicker nas listas críticas.

### Escopo técnico
- Frontend: padronização de estado de erro com retry em páginas críticas de listagem.
- Performance: paginação com `keepPreviousData` para evitar skeleton full a cada troca de página.
- UX: indicador leve de atualização em background (`isFetching`) mantendo conteúdo visível.
- Restrição respeitada: sem alterações no navbar e sem alterações no botão de IA.

### Arquivos impactados no lote
- `src/hooks/use-students.ts`
- `src/hooks/use-student-app.ts`
- `src/hooks/use-admin.ts`
- `src/app/dashboard/students/page.tsx`
- `src/app/dashboard/notifications/page.tsx`
- `src/app/dashboard/admin/users/page.tsx`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Queries paginadas com `placeholderData: keepPreviousData` e `staleTime: 30s` em:
	- alunos,
	- notificações,
	- admin/users.
- Estado de erro com CTA “Tentar novamente” (retry via `refetch`) nas três páginas críticas.
- Indicador de atualização em background com `Loader2` durante `isFetching`, preservando lista já renderizada.

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Mudanças somente de camada de apresentação/query cache (sem alteração de contratos API).
- Sem impacto em auth/payments/schema.
- Risco residual baixo: necessidade de monitorar taxa de refetch em ambiente real para tuning fino futuro.

### Rollout / Rollback
- Rollout: imediato (UX/performance client-side).
- Rollback: remover `keepPreviousData` + blocos de erro/retry das páginas afetadas.

### Métricas de sucesso do lote
- Menor flicker em paginação de listas críticas.
- Erro de carregamento com recovery explícito no UI.
- Melhor continuidade visual em atualizações de dados.

### Status do lote
- **LOTE 012: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 013 — Checklist LGPD por fluxo**

---

## Execução Real — LOTE 013 (20/02/2026)

### Objetivo (1 frase)
Consolidar checklist LGPD por fluxo e operacionalizar os direitos do titular diretamente na experiência de Configurações.

### Escopo técnico
- Governança: matriz LGPD por fluxo com evidências técnicas versionadas.
- Frontend: ações de portabilidade e exclusão/anonimização no painel de Configurações.
- Segurança UX: confirmação forte para ação irreversível de exclusão.
- Performance/UX: sem impacto no navbar e sem alteração no botão de IA.

### Arquivos impactados no lote
- `src/app/dashboard/settings/page.tsx`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/LGPD-FLOW-CHECKLIST.md` (novo)
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Novo card “Privacidade e LGPD” em Configurações com:
	- exportação de dados (`GET /users/me/data-export`) com download JSON,
	- exclusão de conta (`DELETE /users/me`) com confirmação por texto `EXCLUIR`,
	- logout forçado pós-anonimização.
- Checklist LGPD por fluxo publicado com matriz de cobertura e riscos residuais.
- Gate 9 adicionado no `QUALITY-GATES.md` para enforcement de governança LGPD por fluxo.

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Exclusão irreversível protegida por confirmação explícita no UI.
- Portabilidade operacionalizada sem necessidade de suporte manual.
- Risco residual: export JSON pode crescer em contas com alto histórico (avaliar compressão futura).

### Rollout / Rollback
- Rollout: imediato em Configurações (sem quebra de contrato API).
- Rollback: remover card LGPD e manter endpoints backend já existentes.

### Métricas de sucesso do lote
- Direitos LGPD críticos acessíveis no frontend autenticado.
- Checklist por fluxo publicado com evidências.
- Restrições respeitadas (navbar e botão IA inalterados).

### Status do lote
- **LOTE 013: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 014 — Offline básico com PWA (degradação elegante + cache inicial de rotas-chave)**

### Ajuste adicional pré-LOTE 014 (20/02/2026)
- Refinamento visual/performance da navegação com glass navbar:
	- `Header`, `Sidebar` e `Mobile Bottom Nav` com estilo unificado (`nav-glass`, `nav-glass-strong`).
	- Dark mode preservado conforme baseline aprovado.
	- Light mode adaptado para contraste e legibilidade sem hardcodes escuros.
	- Sem mudança funcional de rotas, sem alteração do botão de IA.

---

## Execução Real — LOTE 014 (20/02/2026)

### Objetivo (1 frase)
Entregar baseline offline de PWA com degradação elegante e cache inicial de rotas-chave, sem regressão no fluxo online.

### Escopo técnico
- PWA runtime: registro robusto do Service Worker raiz mesmo quando SDK externo é bloqueado.
- Offline UX: fallback consistente para navegação sem conexão.
- Cache inicial: pre-cache de páginas críticas do dashboard para leitura mínima offline.
- Compatibilidade: manter clientes legados com registro em `/sw.js` funcionando no worker unificado.

### Arquivos impactados no lote
- `src/components/pwa/sw-register.tsx`
- `public/OneSignalSDKWorker.js`
- `public/sw.js`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- `ServiceWorkerRegistration` passou a:
	- tentar `getRegistration('/')` primeiro,
	- registrar `'/OneSignalSDKWorker.js'` automaticamente quando não houver SW ativo,
	- manter rotina de update em foco + intervalo de 30 min com cleanup correto de listeners/timers.
- Worker unificado (`OneSignalSDKWorker.js`) evoluído para `CACHE_VERSION = v7` e app shell com rotas-chave:
	- `/dashboard`, `/dashboard/students`, `/dashboard/workouts`, `/dashboard/assessments`, `/dashboard/notifications`, `/dashboard/settings`, `/dashboard/admin/users`.
- `public/sw.js` convertido em camada de compatibilidade:
	- delega para `importScripts('/OneSignalSDKWorker.js')`,
	- mantém fallback mínimo offline em caso de falha de import.

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Mantida compatibilidade com push OneSignal e operação PWA unificada no mesmo worker.
- Degradação offline preservada mesmo com bloqueio de SDK externo.
- Risco residual: algumas rotas autenticadas podem retornar shell sem dados frescos offline (esperado para baseline).

### Rollout / Rollback
- Rollout: imediato (frontend estático + SW versionado).
- Rollback: reverter `sw-register.tsx`, `OneSignalSDKWorker.js` e `sw.js` para versão anterior.

### Métricas de sucesso do lote
- SW raiz registrado de forma confiável em clientes sem registro prévio.
- Navegação degradada para `/offline` funcional sem conectividade.
- Rotas-chave disponíveis no cache inicial para leitura mínima.

### Status do lote
- **LOTE 014: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 015 — Cache inteligente (camada app)**

---

## Execução Real — LOTE 015 (20/02/2026)

### Objetivo (1 frase)
Implementar cache inteligente na camada app para reduzir latência percebida, minimizar refetch redundante e aquecer dados críticos após autenticação.

### Escopo técnico
- Política central de cache por perfil de dado (listas, detalhes, métricas, realtime).
- Warmup de queries críticas por tipo de usuário após login/hidratação.
- Aplicação do perfil de cache em hooks estratégicas de dashboard.
- Compatibilidade total com auth guard e com baseline offline/PWA do lote anterior.

### Arquivos impactados no lote
- `src/lib/query-cache-policy.ts`
- `src/components/cache/query-warmup.tsx`
- `src/components/providers/index.tsx`
- `src/hooks/use-students.ts`
- `src/hooks/use-student-app.ts`
- `src/hooks/use-admin.ts`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Criada política central `APP_QUERY_CACHE` com perfis:
	- `list` (offline-first, 45s stale, 15min GC),
	- `detail` (offline-first, 60s stale, 20min GC),
	- `stats` (offline-first, 20s stale),
	- `realtime` (online, 15s stale).
- Novo `QueryWarmup` global:
	- prefetch de `notifications/unread` e primeira página de notificações para todos autenticados,
	- prefetch inicial de alunos para personal,
	- prefetch de `admin/stats` e primeira página de `admin/users` para admin/super_admin,
	- rewarm automático em evento `online`.
- Hooks críticas migradas para política central:
	- `useStudents`, `useStudent`,
	- `useNotifications`, `useUnreadCount`, `useStudentEvolution`, `useStudentBadges`,
	- `useAdminStats`, `useAdminUsers`.

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Sem persistência sensível em storage adicional; cache segue no QueryClient em memória.
- Warmup executa somente com usuário autenticado/hidratado.
- Risco residual: tuning fino de TTL por domínio de tela pode evoluir no LOTE 016 (invalidação orientada a eventos).

### Rollout / Rollback
- Rollout: imediato (camada frontend, sem breaking changes de API).
- Rollback: remover `QueryWarmup` e `APP_QUERY_CACHE`, voltando a `staleTime` local anterior.

### Métricas de sucesso do lote
- Menor tempo percebido para primeira abertura de telas críticas autenticadas.
- Menos refetch em foco para listas com baixa volatilidade.
- Base pronta para invalidação orientada a eventos (próximo lote).

### Status do lote
- **LOTE 015: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 016 — Cache orientado a eventos**

---

## Execução Real — LOTE 016 (20/02/2026)

### Objetivo (1 frase)
Evoluir o cache da aplicação para invalidação orientada a eventos de domínio, reduzindo acoplamento entre mutations e chaves de query.

### Escopo técnico
- Criar barramento simples de eventos de cache no frontend.
- Centralizar o mapa de invalidação por tipo de evento.
- Substituir invalidações diretas nas mutations críticas por emissão de eventos.
- Registrar listener global no topo da árvore para manter comportamento consistente.

### Arquivos impactados no lote
- `src/lib/cache-events.ts`
- `src/components/cache/cache-event-listener.tsx`
- `src/components/providers/index.tsx`
- `src/hooks/use-students.ts`
- `src/hooks/use-admin.ts`
- `src/app/dashboard/notifications/page.tsx`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Criado `cache-events.ts` com:
	- tipos de evento (`students:changed`, `notifications:changed`, `admin:*`),
	- `emitCacheEvent(...)`,
	- `invalidateByCacheEvent(...)` com invalidadores centralizados por domínio.
- Criado `CacheEventListener` global para escutar `app:cache-event` e executar invalidação no `QueryClient`.
- `Providers` atualizado para montar `CacheEventListener` global.
- Mutações de Students/Admin e ações de Notifications migradas de `queryClient.invalidateQueries(...)` para `emitCacheEvent(...)`.

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Mudança restrita ao frontend (sem alteração de contrato de API).
- Menor risco de inconsistência de cache por centralização de invalidadores.
- Risco residual: cobertura de eventos ainda incremental para domínios menos críticos (expandir nos próximos lotes conforme necessidade).

### Rollout / Rollback
- Rollout: imediato, sem downtime.
- Rollback: remover listener/event-bus e restaurar invalidações diretas nas mutations afetadas.

### Métricas de sucesso do lote
- Acoplamento reduzido entre mutation e estrutura de query keys.
- Invalidação previsível por evento de domínio.
- Base pronta para fan-out de eventos (ex.: cross-tab) sem editar hooks individualmente.

### Status do lote
- **LOTE 016: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 017 — Notificação + email padronizados**

---

## Execução Real — LOTE 017 (20/02/2026)

### Objetivo (1 frase)
Padronizar eventos de comunicação (notificação/email) com catálogo único de domínio e consistência de payload para reduzir drift entre fluxos.

### Escopo técnico
- Catálogo central de eventos de comunicação.
- Refatoração dos helpers de notificação para usar payload padronizado.
- Base preparada para alinhamento com preferências por canal no lote seguinte.

### Arquivos impactados no lote
- `lib/notification-events.ts`
- `lib/onesignal.ts`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Criado catálogo `resolveNotificationEvent(...)` com payload padrão: `title`, `message`, `link`, `domainType`, `emailTemplate`.
- Helpers de conveniência (`notifyNewWorkout`, `notifyPaymentReceived`, `notifyPaymentOverdue`, `notifyNewStudent`, `notifyAssessmentCompleted`, `notifyTrialExpiring`) migrados para o catálogo.
- `notify(...)` preparado para respeitar canal e tipo via preferências (fallback seguro quando indisponível).

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Mudança backward-compatible, sem quebra de assinatura pública dos helpers.
- Estratégia best-effort preservada para push/in-app.

### Rollout / Rollback
- Rollout: imediato.
- Rollback: reverter `lib/notification-events.ts` e trechos de integração em `lib/onesignal.ts`.

### Métricas de sucesso do lote
- Eventos críticos com payload uniforme.
- Menor risco de inconsistência entre notificações por domínio.

### Status do lote
- **LOTE 017: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 018 — Central de preferências de notificação**

---

## Execução Real — LOTE 018 (20/02/2026)

### Objetivo (1 frase)
Entregar central de preferências de notificação (canais + domínios) integrada em backend e frontend autenticado.

### Escopo técnico
- Migration de preferências de notificação no PostgreSQL.
- Endpoints dedicados para leitura/atualização de preferências.
- UI de Configurações com toggles por canal e por domínio de evento.
- Compatibilidade demo mode atualizada.

### Arquivos impactados no lote
- `migrations/hyperdrive/0011_notification_preferences.sql`
- `lib/notification-preferences.ts`
- `workers/api/notifications.ts`
- `src/hooks/use-notification-preferences.ts`
- `src/app/dashboard/settings/page.tsx`
- `src/lib/mock-api.ts`
- `docs/BACKEND.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Nova tabela `notification_preferences` com defaults seguros e índice por `user_id`.
- Endpoints:
	- `GET /api/v1/notifications/preferences`
	- `PATCH /api/v1/notifications/preferences`
- Auto-criação de preferências no primeiro acesso.
- Card “Central de Preferências” em Configurações com toggles de:
	- canais (`in_app`, `push`, `email`),
	- domínios (`workout`, `payment`, `student`, `assessment`, `marketing`).

### Validação obrigatória (Gate 0)
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Preferências são por usuário autenticado com `authMiddleware`.
- Sem exposição de dados de terceiros.

### Rollout / Rollback
- Rollout: aplicar migration + deploy.
- Rollback: manter tabela e desativar consumo dos endpoints no frontend.

### Métricas de sucesso do lote
- Usuário controla canais/eventos sem suporte manual.
- Defaults previsíveis em conta sem configuração prévia.

### Status do lote
- **LOTE 018: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 019 — Auditoria de segurança web (CSP/CORS/headers)**

---

## Execução Real — LOTE 019 (20/02/2026)

### Objetivo (1 frase)
Estabelecer auditoria reproduzível de segurança web com evidência versionada para CSP/CORS/headers críticos.

### Escopo técnico
- Script operacional de auditoria web.
- Geração de relatório versionado no diretório de execução de lotes.
- Integração com scripts npm para operação contínua.

### Arquivos impactados no lote
- `scripts/run-web-security-audit.mjs`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/WEB-SECURITY-AUDIT.generated.md`
- `package.json`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Comando `npm run ops:web:audit` criado.
- Auditoria executa checks em frontend e API para headers de hardening/CORS.
- Relatório `.generated.md` com resultados por alvo e tabela de conformidade.

### Validação obrigatória (Gate 0)
- `npm run ops:web:audit` ✅
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Evidência operacional objetiva para revisão de headers.
- Gap analysis documentado para correção incremental sem bloquear runtime.

### Rollout / Rollback
- Rollout: imediato (script-only + documentação).
- Rollback: remover script e relatório (não recomendado).

### Métricas de sucesso do lote
- Auditoria reproduzível por comando único.
- Evidência versionada no repositório.

### Status do lote
- **LOTE 019: DONE**
- Go/No-Go: **GO**

### Próximo lote recomendado
- **LOTE 020 — Go/No-Go MVP Produção**

---

## Execução Real — LOTE 020 (20/02/2026)

### Objetivo (1 frase)
Consolidar checklist final e decisão GO/NO-GO para MVP em produção com prontidão de deploy controlado.

### Escopo técnico
- Script de consolidação de Go/No-Go.
- Evidência final gerada e versionada.
- Encerramento da trilha 011–020 com status operacional para deploy.

### Arquivos impactados no lote
- `scripts/generate-go-no-go-report.mjs`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/GO-NO-GO-MVP.generated.md`
- `package.json`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/QUALITY-GATES.md`
- `docs/CHANGELOG.md`
- `docs/ULTRA-PLANO-MVP-PRODUCAO/EXECUCAO-LOTES/LOTE-011-020.md`

### Implementação aplicada
- Comando `npm run ops:go-no-go` criado.
- Relatório final com critérios objetivos (lotes, evidências e gates).
- Encadeamento final de governança para deploy via `npm run cf:deploy`.

### Validação obrigatória (Gate 0)
- `npm run ops:go-no-go` ✅
- `npm run lint` ✅
- `npm run type-check` ✅
- `npx tsc --noEmit -p tsconfig.workers.json` ✅
- `npm run build` ✅
- `npx vitest run tests/api/*.test.ts` ✅ (**47/47**)

### Segurança e operação (Step 4)
- Decisão final registrada com critérios auditáveis.
- Pré-condição de deploy mantida: uso obrigatório de `npm run cf:deploy`.

### Rollout / Rollback
- Rollout: pronto para deploy controlado.
- Rollback: NO-GO explícito em caso de falha em qualquer gate final.

### Métricas de sucesso do lote
- Relatório final GO/NO-GO disponível e rastreável.
- Trilha 011–020 encerrada com governança completa.

### Status do lote
- **LOTE 020: DONE**
- Go/No-Go: **GO**

### Encerramento do bloco 011–020
- **MVP pronto para deploy controlado em produção.**
- Próxima ação operacional: `npm run cf:deploy`.
