{
  "name": "offshoreproz-whatsapp",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsApp Gateway Worker (Unipile) for OffshoreProz — send messages + task-notify formatter",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format pretty",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251228.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.68.1"
  }
}
